// Online C++ compiler to run C++ program online
#include <iostream>

int main() {
    // Write C++ code here
    double nota1, nota2, nota3, promedio;// variable realizada
    count << "Ingrese la nota 1     ",
    cin>>"nota1";
    count << "Ingrese la nota 2     ",
    cin>>"nota2";
    count << "Ingrese la nota 3     ",
    cin>>"nota3";
    promedio=(nota1+nota2+nota3)/3;
    if (promedio>=6)
    
    count << "aprobaste con: "<<promedio;
   else 
    count << "reprobaste con: "<<promedio;
    
    return 0;